---
name: fix
trigger: $fix
loads: [codex-workflow-autopilot, codex-systematic-debugging, codex-test-driven-development, codex-execution-quality-gate]
---

# Workflow Alias: $fix

## Trigger

Alias of `$debug`. Use for bug reports, broken behavior, and root-cause fixes.

## Step Outline

Follow `.workflows/debug.md`. `$fix` and `$debug` are the same repair workflow.

## Exit Criteria

Same as `$debug`.
