# Script Commands

The former `## Core Script Inventory (30)` section moved to `skills/.system/REGISTRY.md`.
Use that registry as the canonical command table for script purpose plus Windows and macOS/Linux invocation paths.
