---
name: codex-design-system
description: Use when applying visual grammar, type, color, composition, and anti-slop to UI work.
load_priority: on-demand
version: "17.0.0"
---

Merged into `codex-frontend-design`. Catalogs remain in `../codex-frontend-design/references/` and still in this skill's `references/` for compatibility. Alias `$design` now loads `codex-frontend-design`.
